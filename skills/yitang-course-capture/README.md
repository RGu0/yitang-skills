# yitang-course-capture

这个目录现在只作为 `meta` 仓库中的只读占位符存在，不再承载该 skill 的实际实现。

实际开发、测试和 git 生命周期已经迁移到独立仓库：

1. 本地工作区：
   `/Users/ruiguo/Documents/0. AI/yitang-course-capture`
2. GitHub：
   `git@github.com:RGu0/yitang-course-capture.git`
3. Codeup：
   `git@codeup.aliyun.com:69d5ce22ad0a337b92d94581/Agent-skills/yitang-course-capture.git`

这个占位目录的职责只有两个：

1. 保持 `meta` 仓库中的技能索引结构稳定
2. 明确提示维护者去独立仓库继续开发

不要再在这里修改脚本、参考资料或 `SKILL.md`。
